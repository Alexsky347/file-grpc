map $sent_http_content_type $expires {
  default off;
  text/html epoch;
  text/css max;
  application/javascript max;
  ~image/ max;
}
server {
  listen 80;
  listen [::]:80;
  # server_name localhost;
  server_name www.ledainalexis.com ledainalexis.com;
  index index.html;
  
  gzip on;
  gzip_min_length 1000;
  gzip_buffers 4 32k;
  gzip_proxied any;
  gzip_types text/plain application/javascript application/x-javascript text/javascript text/xml text/css;
  gzip_vary on;
  gzip_disable “MSIE [1–6]\.(?!.*SV1)”;
  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log;
  
  location /frontend {
    root /usr/share/nginx/html;
    try_files $uri /index.html =404;
  }

  # location / {
  #       root /var/www/certbot;
  # }

  # location / {
  #       return 301 https://ledainalexis.com$request_uri;
  # }
}

# server {
#     listen 443 default_server ssl http2;
#     listen [::]:443 ssl http2;

#     server_name ledainalexis.com;

#     ssl_certificate /etc/nginx/ssl/live/ledainalexis.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/ledainalexis.com/privkey.pem;
    
#     location / {
#     	# ...
#     }
# }